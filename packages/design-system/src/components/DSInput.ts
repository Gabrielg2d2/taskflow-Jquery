/**
 * DSInput - Input do Design System.
 * Placeholder: implementação futura.
 */
export function DSInput(): HTMLInputElement {
  const input = document.createElement("input");
  input.className = "ds-input";
  input.type = "text";
  input.placeholder = "";
  return input;
}
